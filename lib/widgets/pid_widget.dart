import 'package:flutter/material.dart';

class pidWidget extends StatelessWidget {
  const pidWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
      title: const Text("PID Controller Page"),
    ));
  }
}
